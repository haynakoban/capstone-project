import { Container } from '@mui/material';

import ProfileLayout from '../../layout/ProfileLayout';

const Resume = () => {
  return (
    <ProfileLayout>
      <Container maxWidth='me'>resume</Container>
    </ProfileLayout>
  );
};
export default Resume;
