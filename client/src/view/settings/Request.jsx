import { Container } from '@mui/material';

import ProfileLayout from '../../layout/ProfileLayout';

const Request = () => {
  return (
    <ProfileLayout>
      <Container maxWidth='me'>Request</Container>
    </ProfileLayout>
  );
};
export default Request;
