import { Container } from '@mui/material';

import ProfileLayout from '../../layout/ProfileLayout';

const Invitation = () => {
  return (
    <ProfileLayout>
      <Container maxWidth='me'>Invitation</Container>
    </ProfileLayout>
  );
};
export default Invitation;
